select * from all_users;

--knowing about the current user
show user;

--tables
select * from tab;

--screen visibilty
set lines 300 --/
set pages 40   --/

--data
SELECT * FROM employees;
-- sturcture
describe employees;

--statements are related to language : like in sql, insert delete
--commands are related to environment: like in sqlplus set lines 300, set pages 40, sqldeveloper
